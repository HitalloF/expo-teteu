import { Stack } from 'expo-router';

export default function Layout() {
  return (
    <Stack>
      {/* Não é necessário definir as rotas manualmente, o expo-router cuida disso */}
    </Stack>
  );
}
